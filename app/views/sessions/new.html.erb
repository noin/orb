<% form_tag(session_url) do %>
  <p>
    <label for="openid_url">OpenID:</label>
    http:// <%= text_field_tag "openid_url", cookies[:user_url] %>
  </p>

  <p>
    <%= submit_tag 'Sign in', :disable_with => "Signing in&hellip;" %>
  </p>
<% end %>

<% for msg in flash -%>
  <%= msg[0] %>: <%= msg[1] %>
<% end unless flash.empty? -%>